---

# Create an array of dictionaries containing all the hostnames PER-REGION/AZ (e.g. couchbase-dev-sys-euw1-a0-<suffix>).
# Region is shortened via the region_short filter so multi-region clusters keep unique hostnames (GCP/Azure require uniqueness).
- name: get_cluster_hosts_target | Create cluster_hosts_target from the cluster definition in cluster_vars
  ansible.builtin.set_fact:
    cluster_hosts_target: |
      {%- set ns = namespace(res=[]) -%}
      {%- for hosttype in cluster_vars[buildenv].hosttype_vars.keys() -%}
        {%- for region in cluster_vars[buildenv].hosttype_vars[hosttype].region_vars.keys() -%}
          {%- for azname in cluster_vars[buildenv].hosttype_vars[hosttype].region_vars[region].vms_by_zone.keys() -%}
            {%- for azcount in range(0, cluster_vars[buildenv].hosttype_vars[hosttype].region_vars[region].vms_by_zone[azname] | int) -%}
              {%- set ns.res = ns.res + [{
                'hosttype': hosttype,
                'hostname': (cluster_name + '-' +
                             ((hosttype + '-') if (omit_singleton_hosttype_from_hostname is not defined or omit_singleton_hosttype_from_hostname is falsy or (cluster_vars[buildenv].hosttype_vars.keys() | length > 1)) else '') +
                             (((region | clusterverse.clusterverse.region_short) + '-') if (omit_singleton_region_from_hostname is not defined or omit_singleton_region_from_hostname is falsy or (cluster_vars[buildenv].hosttype_vars | json_query('*.region_vars.keys(@)[]')) | unique | length > 1) else '') +
                             (azname | string) + (azcount | string) + '-' +
                             cluster_suffix | string),
                'region': region | string,
                'az_name': azname | string,
                'flavor': cluster_vars[buildenv].hosttype_vars[hosttype].region_vars[region].flavor | default(cluster_vars[buildenv].hosttype_vars[hosttype].flavor),
                'base_image': cluster_vars[buildenv].hosttype_vars[hosttype].region_vars[region].base_image | default(cluster_vars.base_image),
                'auto_volumes': cluster_vars[buildenv].hosttype_vars[hosttype].auto_volumes
              }] -%}
            {%- endfor -%}
          {%- endfor -%}
        {%- endfor -%}
      {%- endfor -%}
      {{ ns.res }}

- name: get_cluster_hosts_target | cluster_hosts_target before cloud-specific augmentation
  debug: msg={{cluster_hosts_target}}
  delegate_to: localhost
  run_once: true

- name: get_cluster_hosts_target | Update cluster_hosts_target base_image (per hosttype) with the base_image used in an existing cluster (when existing cluster)
  block:
    - set_fact: { __orig_cluster_hosts_target: "{{ cluster_hosts_target }}" }         ## NOTE: We cannot use a 'vars:' variable at the block-level in place of a 'set_fact:' variable here, because a 'vars:' assignment only does a shallow-copy, (so cluster_hosts_target becomes the same as __orig_cluster_hosts_target), whereas 'set_fact:' does a deep-copy

    - name: get_cluster_hosts_target | Update cluster_hosts_target base_image (per hosttype+region) with the base_image used in an existing cluster
      ansible.builtin.set_fact:
        cluster_hosts_target: |
          {%- set ns = namespace(res=[]) -%}
          {%- for host in cluster_hosts_target -%}
            {%- set _existing_image = cluster_hosts_state | json_query('[?tagslabels.cv__hosttype==\'' + host.hosttype + '\' && region==\'' + host.region + '\' && tagslabels.cv__lifecycle_state==\'current\' && base_image].base_image') -%}
            {%- set updated_host = host -%}
            {%- if _existing_image | length > 0 -%}
              {%- set updated_host = host | combine({'base_image': _existing_image[0]}) -%}
            {%- endif -%}
            {%- set ns.res = ns.res + [updated_host] -%}
          {%- endfor -%}
          {{ ns.res }}

    - clusterverse.clusterverse.warn_str: msg="get_cluster_hosts_target | Replaced some base images to ensure consistency across hosttype. {{cluster_hosts_target | symmetric_difference(__orig_cluster_hosts_target)}}"
      when: (cluster_hosts_target | symmetric_difference(__orig_cluster_hosts_target) | length > 0)
  when: (cluster_hosts_state | json_query('[?tagslabels.cv__lifecycle_state==\'current\']') | length > 0  and  (override_deprecated_ami is defined and not override_deprecated_ami | bool))


- name: get_cluster_hosts_target | Augment with cloud-specific parameters (if necessary)
  include_tasks: "{{ item__include_tasks }}"
  loop: "{{ query('first_found', params) }}"
  loop_control: { loop_var: item__include_tasks }   #This mechanism to include_tasks only when the file exists, also creates a loop iterator 'item' that it sends to the included tasks.  If they also have loops, we get "The loop variable 'item' is already in use" warning.
  vars: { params: { files: ["get_cluster_hosts_target_{{cluster_vars.cloud_type}}.yml"], skip: true } }


- name: get_cluster_hosts_target | cluster_hosts_target after cloud-specific augmentation
  debug: msg={{cluster_hosts_target}}
  delegate_to: localhost
  run_once: true
